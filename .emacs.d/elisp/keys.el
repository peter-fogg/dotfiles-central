;; Keybindings for existing Emacs functions.

;; Occur for useful regex searching.
(global-set-key (kbd "C-c o") 'occur)

;; Bind align regexp to C-x a r
(global-set-key (kbd "C-x a r") 'align-regexp)
